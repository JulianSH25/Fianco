package Frontend

/**
 * Holds constant values used throughout the application.
 */
object Constants {
    /** The size of each square on the game board in pixels. */
    const val SQUARE_SIZE = 60

    /** The size of each game piece in pixels. */
    const val PIECE_SIZE = 40

    /** The tolerance radius for detecting piece selection in pixels. */
    const val TOLERANCE_RADIUS = 30.0

    /** The initial time for each player's clock in seconds (e.g., 5 minutes). */
    const val INITIAL_TIME_SECONDS = 600
}